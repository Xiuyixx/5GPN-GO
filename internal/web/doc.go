// Package web owns the panel bundle: a real go:embed FS under the "embed"
// build tag, an empty stub otherwise. Copy the dist tree into this directory
// at build time (Makefile release target handles it).
package web
